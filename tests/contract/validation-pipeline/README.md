# Validation Pipeline Contract Tests

This folder contains contract tests for the validation processing pipeline APIs.

Planned coverage:
- Run trigger endpoint (`POST /api/pipeline/run`)
- Run status endpoint (`GET /api/pipeline/{runId}`)
- Validation results endpoints (`GET /api/pipeline/validation-results/{applicationId}` and `GET /api/pipeline/validation-results/{applicationId}/summary`)
